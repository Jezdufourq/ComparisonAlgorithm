# Test 1:
int main(int argc, char* argv[])
{

    vector<vector<int>>odd;
    vector<vector<int>>even;
    vector<vector<int>>onelen;
    vector<vector<int>>rand;
    vector<vector<vector<int>>>rand1;
    vector<vector<int>>median;
    vector<int>large;

    int sims = 10;
    int numArray = 10;

    string oddStr = "oddArray.csv";
    string evenStr = "evenArray.csv";
    string oneStr = "onelenArray.csv";
    string medianStr = "medianArray.csv";
    string randStr = "randArray.csv";
    string largeStr = "largeArray.csv";

    odd = oddArray(sims);
    even = evenArray(sims);
    onelen = onelenArray(sims);
    rand1 = randArray(sims, numArray);
    median = medianArray();
    large = largeArray(100000);

    createCsv2D(oddStr, odd);
    createCsv2D(evenStr, even);
    createCsv2D(oneStr, onelen);
    createCsv3D(randStr, rand1);
    createCsv2D(medianStr, median);
    createCsv1D(largeStr,large);

    print(large);
    print2D(odd);
    print2D(even);
    print2D(onelen);
    print3D(rand1);
    print2D(median);

    return 0;
}
